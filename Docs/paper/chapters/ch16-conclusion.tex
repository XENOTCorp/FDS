\chapter{Conclusion}\label{ch:conclusion}

This thesis formalized Mol, the category of stateful transformations, as the algebraic foundation of zero-allocation network dataplanes. The contribution is a single, compact claim: the engineering practice of high-performance networking (pipeline fusion, batching, hoisting, minimization, zero-copy) is the observable face of a small number of categorical structures, and those structures yield theorems with proofs. Sixty-eight theorems were stated and proved in the body of the thesis. They are collected below as the specification of the framework.

\section{The Theorem Catalog}
The catalog lists all theorems \autoref{thm:1}--\autoref{thm:68}. The tag column marks provenance: \textbf{[N]} denotes a statement original to this thesis, proved with elementary machinery; \textbf{[C]} denotes a classical result restated and fully proved inside Mol, with provenance cited in the text. The statements are compressed to one line; full statements and proofs are in the cited chapters.

\begin{table}[!htbp]
\centering
\caption{Theorems of \autoref{ch:mol}: Mol foundations (\autoref{thm:1}--\autoref{thm:8}).}
\label{tab:thm-mol}
\small
\begin{tabular}{@{}clp{7.2cm}l@{}}
\toprule
Theorem & Tag & Statement & Chapter\\
\midrule
\ref{thm:1} & [C] & Mol is a category: composition of $(S,\mathit{step})$ classes is associative with identity. & \autoref{ch:mol}\\
\ref{thm:2} & [C] & The state-space bijection quotient is well-defined: equivalent machines compose to equivalent machines. & \autoref{ch:mol}\\
\ref{thm:3} & [C] & Mol is symmetric monoidal: tensor $=$ tuple product, unit $=()$, coherent braiding (Mac Lane coherence \cite{maclane}). & \autoref{ch:mol}\\
\ref{thm:4} & [C] & PureMol is equivalent to the category of types and total functions. & \autoref{ch:mol}\\
\ref{thm:5} & [C] & EffMol (Ctx) is isomorphic to the Kleisli category of the state monad on Ctx. & \autoref{ch:mol}\\
\ref{thm:6} & [C] & Behavioral equivalence (bisimulation) is a congruence for $\circ$ and $\otimes$ on lockstep words; Mol$/\!\!\approx$ is symmetric monoidal. & \autoref{ch:mol}\\
\ref{thm:7} & [C] & PureMol and EffMol are closed under tensor; $E \otimes E \subseteq H$ and $E \circ E \subseteq H$ in Mol. & \autoref{ch:mol}\\
\ref{thm:8} & [C] & Pure molecules and their tensor products are deterministic; behavioral equivalence preserves determinism. & \autoref{ch:mol}\\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[!htbp]
\centering
\caption{Theorems of \autoref{ch:lawvere} and \autoref{ch:rewrite}: theories and rewriting (\autoref{thm:9}--\autoref{thm:19}).}
\label{tab:thm-lawvere-rewrite}
\small
\begin{tabular}{@{}clp{7.2cm}l@{}}
\toprule
Theorem & Tag & Statement & Chapter\\
\midrule
\ref{thm:9} & [C] & Stack equations (push;pop $=$ id; pop;push $=$ id when non-empty) hold in every LIFO model and are complete for the free pointer model $\mathbb{Z}$; they fail for FIFO content. & \autoref{ch:lawvere}\\
\ref{thm:10} & [N] & Parser theory: sequence distributes over choice; choice is associative; identity laws; left factoring is sound. & \autoref{ch:lawvere}\\
\ref{thm:11} & [N] & Transport theory: batch\_recv $=$ recv$\otimes\cdots\otimes$recv; the interchange law $(f\otimes g);(h\otimes k) = (f;h)\otimes (g;k)$ holds. & \autoref{ch:lawvere}\\
\ref{thm:12} & [C] & Stack cancellation: an adjacent push;pop pair is removable in every model of $\mathcal{R}$; the rule does not apply to FIFO content. & \autoref{ch:lawvere}\\
\ref{thm:13} & [N] & The ring rewrite system is terminating (lexicographic path measure). & \autoref{ch:rewrite}\\
\ref{thm:14} & [N] & The system is locally confluent; all critical pairs join, hence it is confluent (Newman's lemma \cite{newman}). & \autoref{ch:rewrite}\\
\ref{thm:15} & [N] & Knuth--Bendix completion of the ring theory terminates in a complete system (script-verified, \autoref{app:a}). & \autoref{ch:rewrite}\\
\ref{thm:16} & [N] & Parser left-factoring rewrites are confluent and terminating; left-factored normal forms are unique. & \autoref{ch:rewrite}\\
\ref{thm:17} & [N] & Batch-flattening rewrites are confluent on batch trees; the flattened normal form is unique. & \autoref{ch:rewrite}\\
\ref{thm:18} & [N] & Normalization soundness: $\mathrm{NF} (M) \approx M$ and $\mathrm{cost} (\mathrm{NF} (M)) \le \mathrm{cost} (M)$ for cost-nonincreasing rules. & \autoref{ch:rewrite}\\
\ref{thm:19} & [N] & The runtime equational theory is decidable for finite signatures via normalization. & \autoref{ch:rewrite}\\
\ref{thm:54} & [C] & Modular completion: confluence is modular for disjoint signatures (Toyama); termination of the union requires simple/LPO termination, which the runtime systems satisfy. & \autoref{ch:rewrite}\\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[!htbp]
\centering
\caption{Theorems of \autoref{ch:free} and \autoref{ch:enrich}: free molecules and enrichments (\autoref{thm:20}--\autoref{thm:32}).}
\label{tab:thm-free-enrich}
\small
\begin{tabular}{@{}clp{7.2cm}l@{}}
\toprule
Theorem & Tag & Statement & Chapter\\
\midrule
\ref{thm:20} & [C] & The free symmetric monoidal category $F (\Sigma)$ on atom signature $\Sigma$ exists (initial algebra; accessible categories \cite{adamek}). & \autoref{ch:free}\\
\ref{thm:21} & [C] & Universal property: every molecule over $\Sigma$ factors uniquely (up to coherent iso) through $F (\Sigma)$. & \autoref{ch:free}\\
\ref{thm:22} & [N] & Finiteness: finite $\Sigma$ with bounded types gives finite hom-sets in $F (\Sigma)$. & \autoref{ch:free}\\
\ref{thm:23} & [N] & Every molecule has a unique normal form in $F (\Sigma)$ up to coherence. & \autoref{ch:free}\\
\ref{thm:24} & [N] & Completeness for the runtime theory: $M =_R M'$ iff $\mathrm{NF} (M) = \mathrm{NF} (M')$; $=_R$ is sound for $\approx$ and strictly finer in general. & \autoref{ch:free}\\
\ref{thm:25} & [N] & Cost enrichment is well-defined: additive composition cost, triangle inequality, tensor subadditivity. & \autoref{ch:enrich}\\
\ref{thm:26} & [N] & Global optimality: nonnegative additive costs $\Rightarrow$ min-cost molecules are shortest paths in the finite type graph (Dijkstra complete). & \autoref{ch:enrich}\\
\ref{thm:27} & [N] & Fusion bound: $\mathrm{cost} (\mathrm{fused}\, g\circ f) \le \mathrm{cost} (f)+\mathrm{cost} (g)$, strict when the intermediate type never materializes. & \autoref{ch:enrich}\\
\ref{thm:28} & [N] & Refinement substitution: $f \sqsubseteq f' \wedge g \sqsubseteq g' \Rightarrow g\circ f \sqsubseteq g'\circ f'$; behavior preserved, cost monotone. & \autoref{ch:enrich}\\
\ref{thm:29} & [N] & Maximal refinement: finite refinement lattices have a unique maximal element per behavior class; the most-refined dispatcher is sound and cost-minimal. & \autoref{ch:enrich}\\
\ref{thm:30} & [N] & Complexity closure: atoms in a class $C$ closed under $\circ$ and $\otimes$ yield molecules in $C$. & \autoref{ch:enrich}\\
\ref{thm:31} & [C] & Descriptive complexity: finite-state molecules realize exactly the regular behaviors; minimal-state molecules realize minimal DFAs. & \autoref{ch:enrich}\\
\ref{thm:32} & [N] & Projection reduction: if step depends on state only through $\pi$, then $S_{\min} \cong \pi (S)$. & \autoref{ch:enrich}\\
\ref{thm:53} & [N] & Resource-graded cost enrichment: cache-line, false-sharing, and expected branch-misprediction costs form a graded monoidal enrichment; shortest-path optimality extends. & \autoref{ch:enrich}\\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[!htbp]
\centering
\caption{Theorems of \autoref{ch:kleisli}, \autoref{ch:coalgebra}, and \autoref{ch:trace}: decomposition, minimization, loops (\autoref{thm:33}--\autoref{thm:45}).}
\label{tab:thm-kleisli-coalg-trace}
\small
\begin{tabular}{@{}clp{7.2cm}l@{}}
\toprule
Theorem & Tag & Statement & Chapter\\
\midrule
\ref{thm:33} & [C] & The step of $M = [S,\mathit{step}]$ is a Kleisli morphism of $T_S$; the response is its iterate; conjugate under state-space isomorphism. & \autoref{ch:kleisli}\\
\ref{thm:34} & [C] & Sliding of pure maps for the reactor loop, proved from the machine definition. & \autoref{ch:kleisli}\\
\ref{thm:35} & [C] & Vanishing I--II for the reactor loop; Tr$(\sigma) = \Delta_U$ (delay). Yanking fails for $|U|>1$. & \autoref{ch:kleisli}\\
\ref{thm:36} & [C] & Superposing: Tr$(M)\otimes N = \mathrm{Tr} (M\otimes N)$, proved from the machine definition. & \autoref{ch:kleisli}\\
\ref{thm:37} & [N] & Hoisting shrinks the loop-state footprint; cache footprint of loop state is monotone in state size. & \autoref{ch:kleisli}\\
\ref{thm:38} & [C] & Every finite-state molecule has a minimal state space, unique up to isomorphism (Nerode/Hopcroft \cite{nerode},\cite{hopcroft}). & \autoref{ch:coalgebra}\\
\ref{thm:39} & [C] & Minimization preserves behavior (Nerode quotient equivalence). & \autoref{ch:coalgebra}\\
\ref{thm:40} & [N] & Compositional bounds: $\lvert S_{\min} (g\circ f)\rvert \le \lvert S_f\rvert\cdot\lvert S_g\rvert$; likewise for $f\otimes g$. & \autoref{ch:coalgebra}\\
\ref{thm:41} & [N] & Minimization is compatible with equivalence: $M \approx M' \Rightarrow S_{\min} (M) \cong S_{\min} (M')$. & \autoref{ch:coalgebra}\\
\ref{thm:42} & [C] & A contraction state update (rate $\alpha < 1$) converges to a unique fixed point (Banach \cite{banach}). & \autoref{ch:trace}\\
\ref{thm:43} & [N] & Iteration bound: $k (\alpha,\varepsilon,d_0) = \lceil \ln (\varepsilon (1-\alpha)/d_0)/\ln\alpha\rceil$ iterations suffice; the unrolled loop is within $\varepsilon$, branch-predictable. & \autoref{ch:trace}\\
\ref{thm:44} & [N] & Contraction rates combine: $\max (\alpha_1,\alpha_2)$ under tensor; $\alpha_1\alpha_2$ sequential (sharp). & \autoref{ch:trace}\\
\ref{thm:45} & [C] & Markov updates: total-variation distance contracts by the Dobrushin coefficient \cite{dobroushin}. & \autoref{ch:trace}\\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[!htbp]
\centering
\caption{Theorems of \autoref{ch:operad}, \autoref{ch:linear}, and \autoref{ch:situations}: batching, linearity, situations (\autoref{thm:46}--\autoref{thm:52}).}
\label{tab:thm-operad-linear-situ}
\small
\begin{tabular}{@{}clp{7.2cm}l@{}}
\toprule
Theorem & Tag & Statement & Chapter\\
\midrule
\ref{thm:46} & [N] & Batch flattening: $\mathrm{batch} (\mathrm{batch} (a,b),\mathrm{batch} (c,d)) = \mathrm{batch} (a,b,c,d)$. & \autoref{ch:operad}\\
\ref{thm:47} & [N] & Syscall amortization: $\mathrm{cost} (\mathrm{batch}_n) \le n\cdot\mathrm{cost} (\mathrm{single}) + \mathrm{fixed} (n)$, with $\mathrm{fixed} (n)/n \to 0$ (script-verified, \autoref{app:a}). & \autoref{ch:operad}\\
\ref{thm:48} & [N] & Ring capacity invariant: capacity $2^k$ with bitmask indexing is correct iff in-flight $\le 2^k - 1$. & \autoref{ch:operad}\\
\ref{thm:49} & [N] & Product-state fusion: every pipeline equals a single Mealy machine whose intermediate value is a register. & \autoref{ch:linear}\\
\ref{thm:50} & [N] & Linear atoms plus preallocated rings (occupancy $\le 2^k-1$) $\Rightarrow$ zero hot-path allocation. & \autoref{ch:linear}\\
\ref{thm:55} & [N] & Linear combinator calculus: exact-once variables, non-duplicating reduction, non-growing syntax tree (heap is \ref{thm:50}). & \autoref{ch:linear}\\
\ref{thm:51} & [N] & Situation solvability: bounded types and additive costs $\Rightarrow$ the feasible set of $(A\to B,\Phi,\bar c)$ is finite and decidable; decision-matrix computation terminates. & \autoref{ch:situations}\\
\ref{thm:52} & [N] & Zero-copy roundtrip: parse/serialize inverse on the well-formed domain $\Rightarrow$ parse;serialize $\approx$ id; roundtrip elimination is sound. & \autoref{ch:situations}\\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[!htbp]
\centering
\caption{Theorems of \autoref{ch:breakthroughs} and remaining foundations (\autoref{thm:56}--\autoref{thm:68}).}
\label{tab:thm-boundary-class}
\small
\begin{tabular}{@{}clp{7.2cm}l@{}}
\toprule
Theorem & Tag & Statement & Chapter\\
\midrule
\ref{thm:56} & [N] & Boundary enrichment: syscall count is a well-defined graded cost, lexicographic over userspace cost. & \autoref{ch:breakthroughs}\\
\ref{thm:57} & [N] & Batch fusion: $\beta(\mathrm{batch}_n(R))=1$ for $n\ge 1$, strictly less than the unbunched tensor for $n\ge 2$. & \autoref{ch:breakthroughs}\\
\ref{thm:58} & [N] & Affine crossover: two affine payload costs have at most one threshold; the cheaper realization is unique on each side. & \autoref{ch:breakthroughs}\\
\ref{thm:59} & [N] & Optimization poset: behavior-preserving rewrites of a confluent system form a finite poset under lexicographic $(\beta,c)$ with a unique minimum. & \autoref{ch:breakthroughs}\\
\ref{thm:60} & [N] & Bypass is a retraction: a kernel-bypass path retracts the socket path on the supported device class. & \autoref{ch:breakthroughs}\\
\ref{thm:61} & [N] & Cache soundness: a cached control-plane read with TTL is equivalent to the uncached read on the TTL window. & \autoref{ch:breakthroughs}\\
\ref{thm:62} & [N] & Band contraction: the integer EMA $x\mapsto x+n-x/8$ has attracting band $[8n,8n+7]$. & \autoref{ch:breakthroughs}\\
\ref{thm:63} & [N] & Zero-boundary loops: a typed hot path with $\beta=0$ performs no syscalls in steady state. & \autoref{ch:breakthroughs}\\
\ref{thm:64} & [N] & Route languages are regular; the pattern trie is a deterministic automaton. & \autoref{ch:breakthroughs}\\
\ref{thm:65} & [C] & The route automaton is the Myhill--Nerode refinement of the pattern language. & \autoref{ch:breakthroughs}\\
\ref{thm:66} & [N] & Hybrid matcher selection: the affine crossover decides linear scan versus automaton. & \autoref{ch:breakthroughs}\\
\ref{thm:67} & [N] & The classes $P,E,H$ are the unique congruence for Mol composition and tensor with $E \circ E \subseteq H$ and $E \otimes E \subseteq H$. & \autoref{ch:mol}\\
\ref{thm:68} & [C] & Every Mealy machine $(S,\mathit{step})$ is the molecule $[S,\mathit{step}]$; $F(\Sigma)$ is initial. & \autoref{ch:mol}\\
\bottomrule
\end{tabular}
\end{table}

\section{Limitations}
The framework is deliberately scoped, and its limitations should be stated as precisely as its theorems.

\textbf{Determinism.} Mol is a category of \emph{deterministic} mealy machines; nondeterminism, concurrency, and probabilistic behavior are outside the model. The Markov update theorem \ref{thm:45} is the only probabilistic result, and it is imported from the classical theory rather than internalized. Extending Mol with an effectus or Markov-category structure (\autoref{ch:related}) is natural but was not undertaken here.

\textbf{Finite-state emphasis.} The decidability and finiteness results (\ref{thm:19}, \ref{thm:22}, \ref{thm:51}) assume finite or bounded type signatures. Dataplanes whose state is genuinely unbounded (arbitrary-size connection tables, streaming state) fall outside the normal-form and decision procedures, and the theorems say nothing about them.

\textbf{Proof style.} Proofs are complete and step-justified, with each step stated and discharged in the text. The computational claims of \autoref{ch:testing} are additionally verified by executable tools (see \autoref{app:a}).

\textbf{Measurement.} Cost enrichment makes optimization statements \emph{provable relative to the chosen cost vector}, but the choice of costs (time, memory, cache misses) is empirical, and the strictness claims (\ref{thm:27}) depend on the implementation materializing or eliding intermediate types. The theorems license the optimizations; they do not measure them.

\section{Future Work}
Three directions follow immediately from the limitations and from the open threads noted in the text.

\textbf{Probabilistic and effectful extensions.} Internalizing the Markov structure behind \ref{thm:45}, and giving Mol an effectus-style account of partiality, would extend the framework from deterministic dataplanes to stochastic and best-effort systems while keeping the cost enrichment intact.

\textbf{Information geometry of the trace.} \autoref{ch:trace} noted that the coordinate selection for contraction analysis of state updates has an information-geometric reading; working out the geometry of the loop-state manifolds and its interaction with the iteration bound \ref{thm:43} is left open.

\textbf{Compiler and tooling.} The optimization and testing catalogs of \autoref{app:b} are specifications; building the checker that reads a molecule description, applies the legal optimizations, and discharges the corresponding tests automatically is the engineering continuation of this thesis.

Taken together, the sixty-eight theorems give the dataplane engineer what the empirical literature cannot: a specification against which a networking stack can be certified, with the certification obligations made explicit in \autoref{ch:testing} and \autoref{app:a}.
