# H2/H3 (tokio) datapath bench — atomos-proto — 2026-08-24
# host: xenot | cpu: i5-5200U | tool: examples/bench_h23.rs (h2c + QUIC clients)
# server: atomos-proto --bind 127.0.0.1:8090 (engine=tokio, http2+http3), 4 workers
# load: GET / (static index), 1000 reqs

## H2 (h2c, one connection)
seq:        7953 req/s — p50 120.8us  p90 137.5us  p99 226.3us  p999 306.7us
mux x64:   19505 req/s on a single connection (2.45x sequential — multiplexing works)
holb-proxy: small GET p50 126.3us / p99 330.5us while a 256 KiB upload
            sibling stream is in flight on the SAME connection
            (baseline p50 120.8us — no app-level head-of-line blocking;
            the h2 crate interleaves the streams)
hpack-proxy: raw headers/req 4 B (pseudo-headers excluded by http::Request)
             wire/req FIRST 149 B -> STEADY 12 B after 500 identical
             requests (static+dynamic table hits: ~92% compression)

## H3 (QUIC, one connection)
seq: 5004 req/s — p50 184.8us  p90 246.6us  p99 362.5us  p999 483.7us
# (QPACK wire not measurable: quinn/h3 hide the stream byte counts —
#  the h2 counting-IO wrapper does not exist for the QUIC path)

## notes
- Instrumentation added to atomos src/net/h2serve.rs + h3serve.rs and
  src/kernel/metrics.rs: per-connection wire bytes (CountingIo wrapper),
  raw header bytes/req, body-in bytes, stream counts, RST_STREAM count
  (h2 send_reset + h2 errors with a Reason), exposed via the /metrics
  module (Prometheus text) under atomos_h2_* / atomos_h3_*.
- h2c is prior-knowledge (no TLS). H2-over-TLS uses the same handler.
- Repro: FDS scripts/bench-h23.sh.

## re-run with scheduler + streaming + multi-thread runtime (2026-08-24)
# atomos-proto now: multi_thread runtime, integer scheduler gate on
# every request (firewall + admission, mode=fair), streaming dispatch
# (body chunks fed as they arrive; OutBody::Stream responses).
H2 seq:      7115 req/s (p50 133.4us)   [was 7953 / 120.8us — ~10% gate cost]
H2 mux x64: 18301 req/s                  [was 19505]
H3 seq:      5139 req/s (p50 179.7us)   [was 5004 / 184.8us]
holb-proxy:  p50 140.0us (still muxed, no HOLB)
hpack-proxy: 149 B/req -> 12 B/req (unchanged)
